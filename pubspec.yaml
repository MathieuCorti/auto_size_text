name: auto_size_text
description: Auto-sizing Flutter text with rich text, grouping, TextScaler, intrinsic sizing, and WidgetSpan support.
version: 4.0.0
repository: https://github.com/MathieuCorti/auto_size_text

environment:
  sdk: '>=3.11.0 <4.0.0'
  flutter: '>=3.41.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  leak_tracker_flutter_testing: ^3.0.10
